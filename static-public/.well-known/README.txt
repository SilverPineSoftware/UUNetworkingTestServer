Optional: add Universal Links / App Links files here, for example:

- apple-app-site-association  (often served as application/json)
- assetlinks.json

After adding files, run ./scripts/sync_static_to_s3.sh from the repo root.
